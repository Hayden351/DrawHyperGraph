Replace pseudo zooming with an actual zoom. (Vertices move to proper locations, make sure all elements are zooming)

Create a command line program that can attach to a draw graph window and
run commands to mutate the graph, the window, and the visualization

A central node that has 



